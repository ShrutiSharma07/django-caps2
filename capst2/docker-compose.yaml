version: "3.7"
   
services:
  db:
    image: mysql
    environment:
      - MYSQL_DB=caps2
      - MYSQL_USER=root
      - MYSQL_PASSWORD=Tomodachi25
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db